.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install pointcloud

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv pointcloud
    $ pip install pointcloud
